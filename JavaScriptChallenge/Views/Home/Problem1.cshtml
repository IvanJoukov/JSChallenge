
<div>
    <section>
        @Html.Partial("_Editor", this.ViewData)
        <article id="problemDescription">
            @ViewBag.ProblemDescription
        </article>
    </section>
    Solution: <input type="text" id="proposedSolution" />
    <button type="button" id="evaluate">evaluate</button>
    <button type="button" id="submit">Submit</button>
</div>
<script src="http://cdnjs.cloudflare.com/ajax/libs/ace/1.1.3/ace.js" type="text/javascript" charset="utf-8"></script>
<script type="text/javascript">
    var input = JSON.parse('@Html.Raw(ViewBag.serializedData)');

    debugger;
    $(document).ready(function () {
        $("#submit").click(function () {
            debugger;
            $.post("Problem1", {problemId:@ViewBag.problemInstance, proposedSolution:$("#proposedSolution").val(), solutionCode: editor.getValue()}, function(result) {
                debugger;
            }, "json");
        });

        $("#evaluate").click(function () {
            var result = eval(editor.getValue());
            $("#proposedSolution").val(result);

        });
    });

    var editor = ace.edit("editor");
    editor.setTheme("ace/theme/monokai")
    editor.getSession().setMode("ace/mode/javascript");

</script>
